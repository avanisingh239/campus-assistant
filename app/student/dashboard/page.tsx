// Student Dashboard placeholder (docs/product-spec.md Area A.1). The real
// diff banner / priority feed / "Don't Miss This" lane UI is designed and
// built separately — this is routing-skeleton only.
export default function StudentDashboardPage() {
  return (
    <main>
      <h1>Dashboard</h1>
      <p className="muted">
        Priority feed, diff banner, and discovery lane land here — not built
        in this pass. See docs/product-spec.md Area A.1.
      </p>
    </main>
  );
}
